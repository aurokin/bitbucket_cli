# Roadmap

## Next Commands

- `bb repo clone`
  Clone a Bitbucket repository by workspace/repo with sensible local directory defaults.

- `bb pr merge`
  Merge a pull request from the CLI with explicit strategy and branch cleanup options.

## Follow-Up UX Work

- Interactive prompts for `pr create` when required fields are missing
- More compact human-readable table output for wide terminals
- Better remediation hints for common auth and repo-resolution failures
